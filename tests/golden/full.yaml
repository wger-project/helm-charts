---
# Source: wger/charts/postgres/templates/secureconfig.yaml
apiVersion: v1
kind: Secret
metadata:
  name: golden-postgres
  labels:
    helm.sh/chart: postgres-1.3.11
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "15.18"
    app.kubernetes.io/managed-by: Helm
type: Opaque
data:
  POSTGRES_USER: cG9zdGdyZXM=
  POSTGRES_PASSWORD: cG9zdGdyZXM=
  POSTGRES_DB: d2dlcg==
  USERDB_USER: d2dlcg==
  USERDB_PASSWORD: d2dlcg==
---
# Source: wger/charts/postgres/templates/scripts.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: golden-postgres-scripts
  labels:
    helm.sh/chart: postgres-1.3.11
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "15.18"
    app.kubernetes.io/managed-by: Helm
data:
  01-init-userdb.sh: |
    #!/bin/sh
    create_user()
    {
    psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" -v USERDBNAME="$POSTGRES_DB" -v USERDBUSER="$USERDB_USER" -v USERDBPASSWORD="$USERDB_PASSWORD" <<-EOSQL
      CREATE USER :"USERDBUSER" WITH PASSWORD :'USERDBPASSWORD';
      GRANT ALL PRIVILEGES ON DATABASE :"USERDBNAME" TO :"USERDBUSER";
      ALTER DATABASE :"USERDBNAME" OWNER TO :"USERDBUSER";
    EOSQL
    }
    set -e
    if [ ! -z "$POSTGRES_DB" ] && [ ! -z "$USERDB_USER" ] && [ ! -z "$USERDB_PASSWORD" ]; then
      create_user
    fi
  init.sh: |
    #!/bin/sh
    echo "Start initialization"
    echo "Copy init-userdb script"
    cp /initscripts/01-init-userdb.sh /scripts
    if [ -d /extrascripts ]; then
      echo "Copy extra scripts"
      cp /extrascripts/* /scripts
    fi
    if [ -d /customscripts ]; then
      echo "Copy custom scripts"
      cp /customscripts/* /scripts
    fi
    if [ -d /customconfig ]; then
      echo "Create postgres config"
      cat /customconfig/* >>/configs/postgresql.conf
    fi
    if [ -d /extraconfigs ]; then
      echo "Add extra configs to postgres config"
      cat /extraconfigs/* >>/configs/postgresql.conf
    fi
    echo "Initialization done."
---
# Source: wger/charts/redis/templates/scripts.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: golden-redis-scripts
  labels:
    helm.sh/chart: redis-2.4.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "8.8.0"
    app.kubernetes.io/managed-by: Helm
data:
  init.sh: |
    #!/bin/sh
    REDIS_CONFIG=/data/conf/redis.conf
    SENTINEL_CONFIG=/data/conf/sentinel.conf
    # Clean up old init logs or print them
    cleanup_log() {
      rm -f /data/init.log
      if [ -f "/data/init.log" ]; then
        echo "Detected restart of this instance ($HOSTNAME)"
        echo "## This was the previous log:"
        cat /data/init.log
        echo "## End of previous log"
      fi
    }

    # Log a message during initialization phase
    # $1 - the log message
    log() {
      echo "$(date) $1"
      echo "$(date) $1" >>/data/init.log
    }

    # Creating redis base configuration
    configure_redis_base() {
      log "Creating redis base configuration"
      mkdir -p /data/conf
      rm -f $REDIS_CONFIG

      log "Setting redis server defaults"
      echo "port 6379" >>$REDIS_CONFIG
      echo "protected-mode no" >>$REDIS_CONFIG
      echo "bind 0.0.0.0" >>$REDIS_CONFIG
      echo "dir /data" >>$REDIS_CONFIG
      THISIP="$(getent hosts $HOSTNAME | awk '{ print $1 }')"
      echo "replica-announce-ip ${THISIP}" >>$REDIS_CONFIG
      log "Finished creating base configuration"
    }

    configure_redis_ext() {
      log "Setting extended redis configuration"
      if [ -f /usr/local/etc/redis/redis.conf ]; then
          log "Adding optional redis configuration"
          cat /usr/local/etc/redis/redis.conf >>$REDIS_CONFIG
      fi
      if [ -d /extraredisconfigs ]; then
        log "Adding extra redis configs to redis configuration"
        cat /extraredisconfigs/* >>$REDIS_CONFIG
      fi
    }

    test_master_alive() {
      TEST="$(timeout 2s redis-cli -h $MASTER -p 6379 $REDISUSER $REDISPWD ping)"
      if [ -z "$TEST" ]; then
        log "Master is not alive"
        return 1
      fi
      log "Master is alive"
      return 0
    }

    test_valid_dns() {
      KNOWN_HOSTS="$(getent hosts golden-redis-headless.golden-ns.svc.cluster.local | awk '{ print $1 }')"
      log "## Known hosts for headless service golden-redis-headless.golden-ns.svc.cluster.local:"
      for AHOSTIP in $KNOWN_HOSTS; do
        AHOSTNAME=$(getent hosts $AHOSTIP | awk '{print $2}')
        log "${AHOSTIP} ${AHOSTNAME}"
      done
      log "#####"
      MASTERENTRY="$(getent hosts $MASTER | awk '{ print $1 }')"
    }

    configure_sentinel() {
      log "Configuring sentinel server..."
      if [ -z "$MASTER" ]; then
        MASTER="$(getent hosts $HOSTNAME | awk '{ print $1 }')"
        log "No master found - Configuring sentinel for master $HOSTNAME with address $MASTER"
        echo "sentinel monitor redisha $MASTER 6379 2" >>$SENTINEL_CONFIG
      else
        log "Redis master was found - Configuring sentinel for master address $MASTER"
        echo "sentinel monitor redisha $MASTER 6379 2" >>$SENTINEL_CONFIG
      fi
      echo "sentinel down-after-milliseconds redisha 30000" >>$SENTINEL_CONFIG
      echo "sentinel failover-timeout redisha 180000" >>$SENTINEL_CONFIG
    }

    configure_sentinel_ext() {
      log "Setting extended sentinel configuration"
      if [ -f /usr/local/etc/redis/sentinel.conf ]; then
          log "Adding optional sentinel configuration settings"
          cat /usr/local/etc/redis/sentinel.conf >>$SENTINEL_CONFIG
      fi
      if [ -d /extrasentinelconfigs ]; then
        log "Adding extra sentinel configs to sentinel configuration"
        cat /extrasentinelconfigs/* >>$SENTINEL_CONFIG
      fi
      log "Configuring sentinel server finished."
    }

    configure_redis_cluster() {
      log "Determining authentication parameters"
      if [ -f $REDIS_CONFIG ]; then
        REDISUSER=$(cat $REDIS_CONFIG | grep masteruser | awk '{ print $2 }')
        if [ -n "$REDISUSER" ]; then
          REDISUSER="--user $REDISUSER"
        fi
        REDISPWD=$(cat $REDIS_CONFIG | grep masterauth | awk '{ print $2 }')
        if [ -n "$REDISPWD" ]; then
          REDISPWD="--pass $REDISPWD"
        fi
      fi
      if [ -f $SENTINEL_CONFIG ]; then
        SENTINELUSER=$(cat $SENTINEL_CONFIG | grep "sentinel sentinel-user" | awk '{ print $3 }')
        if [ -n "$SENTINELUSER" ]; then
          SENTINELUSER="--user $SENTINELUSER"
        fi
        SENTINELPWD=$(cat $SENTINEL_CONFIG | grep "sentinel sentinel-pass" | awk '{ print $3 }')
        if [ -n "$SENTINELPWD" ]; then
          SENTINELPWD="--pass $SENTINELPWD"
        fi
      fi
      log "Try to resolve cluster service.."
      SERVICE="$(getent hosts golden-redis)"
      if [ -z "$SERVICE" ]; then
        log "Can't resolve service - Will restart after DNS failure wait"
        sleep 15
        log "Restart"
        exit 1
      else
        log "Service resolved: ${SERVICE}"
      fi
      log "Try to detect cluster master.."
      MASTER="$(timeout 2s redis-cli -h golden-redis -p 26379 $SENTINELUSER $SENTINELPWD sentinel get-master-addr-by-name redisha | head -n 1)"
      if [ -z "$MASTER" ]; then
        log "No master found - This instance ($HOSTNAME) will be master now"
      else
        log "Redis master was found with address $MASTER - Checking host dns entry"
        test_valid_dns
        if [ -z "$MASTERENTRY" ]; then
          log "No valid DNS entry found!"
          if [ -f "/data/failover_restart" ]; then
            rm /data/failover_restart
            log "Forcing failover now"
            redis-cli -h golden-redis -p 26379 $SENTINELUSER $SENTINELPWD sentinel failover redisha
          else
            log "Waiting for failover before restart"
            sleep 35
            touch /data/failover_restart
          fi
          log "Restart"
          exit 1
        else
          MASTER=$MASTERENTRY
          log "$MASTER has valid DNS entry"
          log "Checking if master is alive"
          if ! test_master_alive; then
            if [ -f "/data/failover_restart" ]; then
              rm /data/failover_restart
              log "Master is still dead! - forcing failover and retry pod initialization"
              redis-cli -h golden-redis -p 26379 $SENTINELUSER $SENTINELPWD sentinel failover redisha
            else
              log "Dead master at address $MASTER detected! - waiting for failover"
              sleep 35
              touch /data/failover_restart
            fi
            log "Restart"
            exit 1
          else
            rm -f /data/failover_restart
            log "Setting this instance ($HOSTNAME) as replicaof $MASTER"
            echo "replicaof $MASTER 6379" >>$REDIS_CONFIG
          fi
        fi
      fi
    }

    log "Creating configuration..."
    mkdir -p /data/conf
    cleanup_log
    rm -f $REDIS_CONFIG
    configure_redis_base
    configure_redis_ext
    log "Done."
---
# Source: wger/templates/configmap-nginx.yaml
kind: ConfigMap
apiVersion: v1
metadata:
  name: golden-nginx-configmap
data:
  wger-app.conf: |
    upstream app_server {
      # fail_timeout=0 means we always retry an upstream even if it failed
      # to return a good HTTP response <- according to gunicorn doc
      server golden-app.golden-ns.svc.cluster.local:8000 fail_timeout=0;
      zone upstreams 64K;
      keepalive 2;
    }
    # Used by the /ps/ proxy below.
    upstream powersync {
        server golden-powersync-api.golden-ns.svc.cluster.local:8080;
    }

    # JSON access log format compatible with the Loki/Alloy parser
    # (same field names as the Caddy parser, so the same dashboard works).
    log_format json_access escape=json
        '{'
            '"ts":"$time_iso8601",'
            '"client_ip":"$remote_addr",'
            '"request_method":"$request_method",'
            '"request_uri":"$request_uri",'
            '"request_proto":"$server_protocol",'
            '"request_host":"$host",'
            '"status":$status,'
            '"size":$body_bytes_sent,'
            '"duration":$request_time,'
            '"user_agent":"$http_user_agent",'
            '"referer":"$http_referer"'
        '}';
    
    # Universal X-Forwarded-Proto handling for all deployment scenarios
    # - Uses upstream value when behind reverse proxy (Traefik, Caddy, etc.)
    # - Falls back to nginx's scheme for direct connections and port forwarding
    map $http_x_forwarded_proto $final_forwarded_proto {
        default $http_x_forwarded_proto;
        ''      $scheme;
    }

    # webserver
    server {
      listen 80;

      # set the correct host(s) for your site
      server_name golden.example.com;

      access_log /dev/stdout json_access;

      location / {
        proxy_pass http://app_server;

        # Standard proxy headers
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $final_forwarded_proto;
        proxy_set_header X-Forwarded-Host $http_host;

        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_redirect off;
        proxy_http_version 1.1;

        # Timeouts for WebSocket connections
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
      }

      # Reverse proxy for the PowerSync service, used by the mobile app for offline mode
      location /ps/ {
        proxy_pass http://powersync/;

        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $final_forwarded_proto;

        # WebSocket upgrade (used by the diagnostics app and the Web SDK)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Sync streams are long-lived (HTTP/2 streaming POST) and must not
        # be buffered or cut off by short timeouts.
        proxy_buffering off;
        proxy_read_timeout 1d;
        proxy_send_timeout 1d;
      }

      location /static/ {
        alias /wger/static/;

        # Files in the static folder are preprocessed by collectstatic and have a hash
        # in their names, so we can cache longer
        add_header Cache-Control "public, max-age=31536000, immutable" always;
        add_header Vary "Accept-Encoding" always;
      }

      location /media/ {
        alias /wger/media/;
      }

      # Increase max body size to allow for video uploads
      client_max_body_size 100M;
    }
---
# Source: wger/templates/configmap-postgres.yaml
# extra init script for the in-cluster postgres (mounted via the
# postgres.extraScripts value): the wger DB user needs superuser rights for
# some django migrations and for the powersync storage setup
#
# NOTE: deliberately NOT release-prefixed — the name must match the static
# postgres.extraScripts value, and subchart values cannot be templated.
# Consequence: two releases sharing a namespace conflict on this ConfigMap.
kind: ConfigMap
apiVersion: v1
metadata:
  name: wger-pg-init
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
data:
  40-grantSuperuser.sql: |
    ALTER USER wger WITH SUPERUSER;
---
# Source: wger/templates/configmap-powersync.yaml
kind: ConfigMap
apiVersion: v1
metadata:
  name: golden-powersync-configmap
data:
  powersync.yaml: |
    # yaml-language-server: $schema=../schema/schema.json

    # Note that this example uses YAML custom tags for environment variable substitution.
    # Using `!env [variable name]` will substitute the value of the environment variable named
    # [variable name].
    #
    # Only environment variables with names starting with `PS_` can be substituted.
    #
    # e.g. With the environment variable `export PS_STORAGE_MONGO_URI=mongodb://localhost:27017`
    # and YAML code:
    #  uri: !env PS_STORAGE_MONGO_URI
    # The YAML will resolve to:
    #  uri: mongodb://localhost:27017
    #
    # If using VS Code see the `.vscode/settings.json` definitions which define custom tags.

    # migrations:
    #   # Migrations run automatically by default.
    #   # Setting this to true will skip automatic migrations.
    #   # Migrations can be triggered externally by altering the container `command`.
    #   disable_auto_migration: true

    # Settings for telemetry reporting
    # See https://docs.powersync.com/self-hosting/telemetry
    telemetry:
      disable_telemetry_sharing: true

      # Expose prometheus metrics on this port
      prometheus_port: 9090

    # Settings for source database replication
    replication:
      # Specify database connection details
      # Note only 1 connection is currently supported
      # Multiple connection support is on the roadmap
      connections:
        - type: postgresql
          uri: !env PS_DATABASE_URI

          # Or use individual params
          # hostname: db # From the Docker Compose service name
          # port: 5432
          # database: postgres
          # username: postgres
          # password: mypassword

          # SSL settings
          sslmode: disable # 'verify-full' (default) or 'verify-ca' or 'disable'
          # 'disable' is OK for local/private networks, not for public networks


    # Connection settings for sync bucket storage
    storage:
      type: postgresql
      uri: !env PS_STORAGE_PG_URI
      sslmode: disable

    # The port which the PowerSync API server will listen on
    port: !env PS_PORT

    # Specify sync rules
    sync_rules:
      path: sync_rules.yaml

    # Client (application end user) authentication settings
    client_auth:
      allow_local_jwks: true
      jwks_uri: !env PS_JWKS_URL

      # JWKS audience
      audience: ["powersync"]

    # System-level configuration options
    system:
      # Service logging configuration
      logging:
        # Log level for the service logs
        level: info #  'silly', 'debug', 'verbose', 'http', 'info', 'warn', 'error'
        format: text # 'json' or 'text'

  sync_rules.yaml: |
    # Note that changes to this file are not watched.
    # The service needs to be restarted for changes to take effect.

    # Warning: a user may have at most 1000 buckets, i.e. parameter-query results
    # summed across all streams. This counts the *parameter* rows, not the data
    # rows inside a bucket (a single bucket can hold any number of rows). For a
    # stream with a `with:` CTE the count is the number of rows the CTE returns,
    # so for `user_ingredients` below that is the number of distinct ingredients
    # a user has ever referenced. Exceeding the limit is a hard error
    # (PSYNC_S2305 "Too many parameter query results").
    # See https://docs.powersync.com/sync/rules/parameter-queries
    #
    # Streams are split by update frequency (cold / medium / hot) so that
    # bucket compaction can collapse the head of hot buckets without being
    # blocked by long-lived rows from cold tables.
    #
    # For details, see the documentation:
    # https://docs.powersync.com/sync/streams/overview
    # https://docs.powersync.com/maintenance-ops/compacting-buckets

    config:
      edition: 3

    streams:
      # Global reference data, shared by all users, changes rarely enough
      core:
        auto_subscribe: true
        queries:
          - SELECT * FROM core_language
          - SELECT * FROM core_license
          - SELECT * FROM core_repetitionunit
          - SELECT * FROM core_weightunit
          - SELECT * FROM exercises_exercise
          - SELECT * FROM exercises_translation
          - SELECT * FROM exercises_alias
          - SELECT * FROM exercises_exercisecomment
          - SELECT * FROM exercises_muscle
          - SELECT * FROM exercises_exercise_muscles
          - SELECT * FROM exercises_exercise_muscles_secondary
          - SELECT * FROM exercises_equipment
          - SELECT * FROM exercises_exercise_equipment
          - SELECT * FROM exercises_exercisecategory
          - SELECT * FROM exercises_exerciseimage
          - SELECT * FROM exercises_exercisevideo

      # COLD, per-user data that almost never changes after creation.
      user_profile:
        auto_subscribe: true
        queries:
          - SELECT * FROM core_userprofile WHERE CAST(user_id AS TEXT) = auth.user_id()
          - SELECT * FROM gallery_image WHERE CAST(user_id AS TEXT) = auth.user_id()

      # COLD but potentially large, only the per-user *filter set* changes when the user logs new foods
      user_ingredients:
        auto_subscribe: true
        with:
          user_ingredients: |
            SELECT DISTINCT nutrition_synced_ingredient.id
            FROM nutrition_synced_ingredient
            WHERE nutrition_synced_ingredient.id IN (
                    SELECT nutrition_logitem.ingredient_id FROM nutrition_logitem
                    JOIN nutrition_nutritionplan ON nutrition_logitem.plan_id = nutrition_nutritionplan.id
                    WHERE CAST(nutrition_nutritionplan.user_id AS TEXT) = auth.user_id())
              OR nutrition_synced_ingredient.id IN (
                    SELECT nutrition_mealitem.ingredient_id FROM nutrition_mealitem
                    JOIN nutrition_meal ON nutrition_mealitem.meal_id = nutrition_meal.id
                    JOIN nutrition_nutritionplan ON nutrition_meal.plan_id = nutrition_nutritionplan.id
                    WHERE CAST(nutrition_nutritionplan.user_id AS TEXT) = auth.user_id())
        queries:
          - SELECT * FROM nutrition_synced_ingredient AS nutrition_ingredient WHERE id IN user_ingredients
          - |
            SELECT nutrition_image.* FROM nutrition_image
            WHERE nutrition_image.ingredient_id IN user_ingredients
          - |
            SELECT nutrition_ingredientweightunit.* FROM nutrition_ingredientweightunit
            WHERE nutrition_ingredientweightunit.ingredient_id IN user_ingredients

      # MEDIUM. Edited e.g. when the user builds or edits their routine or nutrition plan,
      # but not on every workout.
      user_planning:
        auto_subscribe: true
        queries:
          # Routines (templates excluded)
          - SELECT * FROM manager_routine WHERE CAST(user_id AS TEXT) = auth.user_id() AND is_template = FALSE

          # Measurements
          - SELECT * FROM measurements_category WHERE CAST(user_id AS TEXT) = auth.user_id()
          - |
            SELECT measurements_measurement.*
            FROM measurements_measurement
            INNER JOIN measurements_category
              ON measurements_measurement.category_id = measurements_category.id
            WHERE CAST(measurements_category.user_id AS TEXT) = auth.user_id()

          # Nutrition plan structure (not the log items)
          - SELECT * FROM nutrition_nutritionplan WHERE CAST(user_id AS TEXT) = auth.user_id()
          - |
            SELECT nutrition_meal.*
            FROM nutrition_meal
            JOIN nutrition_nutritionplan
              ON nutrition_meal.plan_id = nutrition_nutritionplan.id
            WHERE CAST(nutrition_nutritionplan.user_id AS TEXT) = auth.user_id()
          - |
            SELECT nutrition_mealitem.*
            FROM nutrition_mealitem
            JOIN nutrition_meal
              ON nutrition_mealitem.meal_id = nutrition_meal.id
            JOIN nutrition_nutritionplan
              ON nutrition_meal.plan_id = nutrition_nutritionplan.id
            WHERE CAST(nutrition_nutritionplan.user_id AS TEXT) = auth.user_id()


      # HOT. Generates  one or more new rows per workout / meal. Compaction has the
      # biggest impact here, so it must stay isolated from the other streams above
      user_activity:
        auto_subscribe: true
        queries:
          # Weight tracking
          - SELECT uuid AS id, weight, date, user_id FROM weight_weightentry WHERE CAST(user_id AS TEXT) = auth.user_id()

          # Workout sessions and per-set logs
          - SELECT * FROM manager_workoutsession WHERE CAST(user_id AS TEXT) = auth.user_id()
          - SELECT * FROM manager_workoutlog WHERE CAST(user_id AS TEXT) = auth.user_id()

          # Nutrition log entries
          - |
            SELECT nutrition_logitem.*
            FROM nutrition_logitem
            JOIN nutrition_nutritionplan
              ON nutrition_logitem.plan_id = nutrition_nutritionplan.id
            WHERE CAST(nutrition_nutritionplan.user_id AS TEXT) = auth.user_id()
---
# Source: wger/charts/redis/templates/pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: golden-redis
  labels:
    helm.sh/chart: redis-2.4.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "8.8.0"
    app.kubernetes.io/managed-by: Helm
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
---
# Source: wger/templates/persistent-volume-claim.yaml
# yamllint disable rule:document-start
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: golden-media
spec:
  accessModes:
    - "ReadWriteMany"
  resources:
    requests:
      storage: "8Gi"
---
# Source: wger/templates/persistent-volume-claim.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: golden-static
spec:
  accessModes:
    - "ReadWriteMany"
  resources:
    requests:
      storage: "8Gi"
---
# Source: wger/templates/persistent-volume-claim.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: golden-celery-beat
spec:
  accessModes:
    - "ReadWriteMany"
  resources:
    requests:
      storage: "1Gi"
---
# Source: wger/charts/postgres/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: golden-postgres
  labels:
    helm.sh/chart: postgres-1.3.11
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "15.18"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: golden
---
# Source: wger/charts/redis/templates/service-internal.yaml
apiVersion: v1
kind: Service
metadata:
  name: golden-redis-headless
  labels:
    helm.sh/chart: redis-2.4.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "8.8.0"
    app.kubernetes.io/managed-by: Helm
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: golden
---
# Source: wger/charts/redis/templates/services.yaml
apiVersion: v1
kind: Service
metadata:
  name: golden-redis
  labels:
    helm.sh/chart: redis-2.4.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "8.8.0"
    app.kubernetes.io/managed-by: Helm
spec:
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  type: ClusterIP
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: golden
---
# Source: wger/templates/service.yaml
# nginx
apiVersion: v1
kind: Service
metadata:
  name: golden-http
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden-http
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app.kubernetes.io/name: golden-nginx
---
# Source: wger/templates/service.yaml
# wger django app
apiVersion: v1
kind: Service
metadata:
  name: golden-app
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden-app
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
spec:
  type: ClusterIP
  ports:
    - name: app
      protocol: TCP
      port: 8000
      targetPort: 8000
  selector:
    app.kubernetes.io/name: golden-app
---
# Source: wger/templates/service.yaml
# powersync api
apiVersion: v1
kind: Service
metadata:
  name: golden-powersync-api
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden-powersync-api
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
spec:
  type: ClusterIP
  ports:
    - name: api
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: golden-powersync
---
# Source: wger/templates/service.yaml
# powersync prometheus exporter
apiVersion: v1
kind: Service
metadata:
  name: golden-powersync-prometheus
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden-powersync-prometheus
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
spec:
  type: ClusterIP
  ports:
    - name: app
      protocol: TCP
      port: 9090
      targetPort: 9090
  selector:
    app.kubernetes.io/name: golden-powersync
---
# Source: wger/charts/redis/templates/statefulset.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: golden-redis
  labels:
    helm.sh/chart: redis-2.4.0
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "8.8.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
      app.kubernetes.io/instance: golden
  template:
    metadata:
      annotations:
        checksum/customconfig: 01ba4719c80b6fe911b091a7c05124b64eeece964e09c058ef8f9805daca546b
        checksum/scripts: 73c1bb45f96ae81dc3f3c89a8681c917995bde65ba7fb14a4fe83de2ab3ed72a
      labels:
        app.kubernetes.io/name: redis
        app.kubernetes.io/instance: golden
    spec:
      serviceAccountName: default
      securityContext:
        fsGroup: 999
        supplementalGroups:
        - 999
      initContainers:
        - name: redis-init
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 999
            runAsNonRoot: true
            runAsUser: 999
          image: "docker.io/redis:8.8.0"
          imagePullPolicy: IfNotPresent
          envFrom:
          volumeMounts:
            - mountPath: /data
              name: redis-data
            - name: scripts
              mountPath: /scripts
          command: [ "/scripts/init.sh" ]
      containers:
        - name: redis-server
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 999
            runAsNonRoot: true
            runAsUser: 999
          image: "docker.io/redis:8.8.0"
          imagePullPolicy: IfNotPresent
          ports:
            - name: redis
              containerPort: 6379
              protocol: TCP
          startupProbe:
            exec:
              command:
                - sh
                - -c
                - redis-cli ping
            initialDelaySeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
            successThreshold: 1
            periodSeconds: 10              
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - redis-cli ping
            initialDelaySeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - redis-cli ping
            initialDelaySeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
            periodSeconds: 10
          command:
            - redis-server
          args:
            - /data/conf/redis.conf
          envFrom:
          volumeMounts:
            - name: redis-data
              mountPath: /data
      volumes:
        - name: scripts
          configMap:
            name: golden-redis-scripts
            defaultMode: 0555
        - name: redis-data
          persistentVolumeClaim:
            claimName: golden-redis
---
# Source: wger/templates/deployment-celery.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: golden-celery
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
spec:
  replicas: 1
  strategy:
    # kills all old Pods first
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: golden-celery
  template:
    metadata:
      labels:
        app.kubernetes.io/name: golden-celery
      annotations:        
        checksum/secrets: d0c5d15aecc7a1765236648dccd0e733aff527b1194051d18479f566e1bf5c7b
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        # celery beat
        - name: celery-beat
          image: "docker.io/wger/server:2.6"
          imagePullPolicy: IfNotPresent
          env:            
            # get default env
            # get list of custom defined env
            # get default env list without custom ones (override)
            # merge default env with values env
            # ouput list of dict
            - name: TZ
              value: "UTC"
            - name: TIME_ZONE
              value: "UTC"
            - name: ENABLE_EMAIL
              value: "True"
            - name: EMAIL_HOST
              value: "smtp.example.com"
            - name: EMAIL_PORT
              value: "587"
            - name: EMAIL_HOST_USER
              value: "wger@example.com"
            - name: FROM_EMAIL
              value: "wger@example.com"
            - name: DJANGO_PERFORM_MIGRATIONS
              value: "True"
            - name: DJANGO_DB_ENGINE
              value: "django.db.backends.postgresql"
            - name: DJANGO_CACHE_BACKEND
              value: "django_redis.cache.RedisCache"
            - name: DJANGO_CACHE_LOCATION
              value: "redis://golden-redis:6379/1"
            - name: DJANGO_CACHE_CLIENT_CLASS
              value: "django_redis.client.DefaultClient"
            - name: DJANGO_CACHE_TIMEOUT
              value: "1296000"
            - name: EXERCISE_CACHE_TTL
              value: "2419200"
            - name: SITE_URL
              value: "https://golden.example.com"
            - name: CSRF_TRUSTED_ORIGINS
              value: "http://golden.example.com,https://golden.example.com,http://127.0.0.1,https://127.0.0.1,http://localhost,https://localhost"
            - name: DJANGO_DEBUG
              value: "False"
            - name: DJANGO_MEDIA_ROOT
              value: "/home/wger/media"
            - name: NUMBER_OF_PROXIES
              value: "1"
            - name: AXES_ENABLED
              value: "True"
            - name: AXES_LOCKOUT_PARAMETERS
              value: "ip_address"
            - name: AXES_FAILURE_LIMIT
              value: "10"
            - name: AXES_COOLOFF_TIME
              value: "30"
            - name: AXES_IPWARE_PROXY_COUNT
              value: "1"
            - name: AXES_IPWARE_META_PRECEDENCE_ORDER
              value: "HTTP_X_FORWARDED_FOR,REMOTE_ADDR"
            - name: AXES_HANDLER
              value: "axes.handlers.cache.AxesCacheHandler"
            - name: ACCESS_TOKEN_LIFETIME
              value: "10"
            - name: REFRESH_TOKEN_LIFETIME
              value: "24"
            - name: WGER_USE_GUNICORN
              value: "True"
            - name: GUNICORN_CMD_ARGS
              value: "--timeout 240 --workers 4 --worker-class gthread --threads 3 --forwarded-allow-ips * --proxy-protocol True --access-logformat='%(h)s %(l)s %({client-ip}i)s %(l)s %({x-real-ip}i)s %(l)s %({x-forwarded-for}i)s %(l)s %(t)s \"%(r)s\" %(s)s %(b)s \"%(f)s\" \"%(a)s\"' --access-logfile - --error-logfile -"
            - name: MIN_ACCOUNT_AGE_TO_TRUST
              value: "21"
            - name: ALLOW_REGISTRATION
              value: "False"
            - name: ALLOW_GUEST_USERS
              value: "False"
            - name: WGER_INSTANCE
              value: "https://wger.de"
            - name: ALLOW_UPLOAD_VIDEOS
              value: "True"
            - name: SYNC_EXERCISES_ON_STARTUP
              value: "False"
            - name: DOWNLOAD_EXERCISE_IMAGES_ON_STARTUP
              value: "False"
            - name: USE_CELERY
              value: "True"
            - name: SYNC_EXERCISES_CELERY
              value: "true"
            - name: SYNC_EXERCISE_IMAGES_CELERY
              value: "true"
            - name: SYNC_EXERCISE_VIDEOS_CELERY
              value: "true"
            - name: DOWNLOAD_INGREDIENTS_FROM
              value: "WGER"
            - name: CELERY_WORKER_CONCURRENCY
              value: "4"
            - name: CACHE_API_EXERCISES_CELERY
              value: "true"
            - name: CACHE_API_EXERCISES_CELERY_FORCE_UPDATE
              value: "true"          
            - name: DJANGO_DB_HOST
              value: "golden-postgres"
            - name: DJANGO_DB_PORT
              value: "5432"
            - name: DJANGO_DB_USER
              valueFrom:
                secretKeyRef:
                  name:  golden-postgres
                  key: "USERDB_USER"
            - name: DJANGO_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "USERDB_PASSWORD"
            - name: DJANGO_DB_DATABASE
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "POSTGRES_DB"          
            - name: JWT_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "private-key"
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "public-key"
            # This is the path (inside the container) to the YAML config file
            # Alternatively the config path can be specified in the command
            # e.g:
            #   command: ['start', '-r', 'unified', '-c', '/config/powersync.yaml']
            #
            # The config file can also be specified in Base 64 encoding
            # e.g.: Via an environment variable
            #   POWERSYNC_CONFIG_B64: [base64 encoded content]
            # or e.g.: Via a command line parameter
            #    command: ['start', '-r', 'unified', '-c64', '[base64 encoded content]']
            - name: POWERSYNC_CONFIG_PATH
              value: "/config/powersync.yaml"
            # Sync rules can be specified as base 64 encoded YAML
            # e.g: Via an environment variable
            # POWERSYNC_SYNC_RULES_B64: "[base64 encoded sync rules]"
            # or e.g.: Via a command line parameter
            #     command: ['start', '-r', 'unified', '-sync64', '[base64 encoded content]']
            - name: PS_JWKS_URL
              value: "http://golden-http:80/api/v2/powersync-keys"
            - name: PS_PORT
              value: "8080"
            - name: POWERSYNC_URL_PATH
              value: "ps"
            # ps database settings
            - name: PS_DB_USER
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "user"
            - name: PS_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "pw"
            - name: PS_STORAGE_PG_URI
              value: "postgres://$(PS_DB_USER):$(PS_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"
            - name: PS_DATABASE_URI
              value: "postgres://$(DJANGO_DB_USER):$(DJANGO_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"          
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-django"
                  key: "secret-key"
            - name: EMAIL_HOST_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-mail"
                  key: "mail-password"
            - name: DJANGO_CACHE_CLIENT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-redis"
                  key: "redis-password"
            - name: CELERY_BROKER
              value: "redis://:$(DJANGO_CACHE_CLIENT_PASSWORD)@golden-redis:6379/2"
            - name: CELERY_BACKEND
              value: "redis://:$(DJANGO_CACHE_CLIENT_PASSWORD)@golden-redis:6379/2"
            - name: CELERY_FLOWER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-flower"
                  key: "password"
          command: ["/start-beat"]
          volumeMounts:
            - name: wger-celery-beat
              mountPath: /home/wger/beat
              readOnly: false
        # celery flower
        - name: celery-flower
          image: "docker.io/wger/server:2.6"
          imagePullPolicy: IfNotPresent
          env:            
            # get default env
            # get list of custom defined env
            # get default env list without custom ones (override)
            # merge default env with values env
            # ouput list of dict
            - name: TZ
              value: "UTC"
            - name: TIME_ZONE
              value: "UTC"
            - name: ENABLE_EMAIL
              value: "True"
            - name: EMAIL_HOST
              value: "smtp.example.com"
            - name: EMAIL_PORT
              value: "587"
            - name: EMAIL_HOST_USER
              value: "wger@example.com"
            - name: FROM_EMAIL
              value: "wger@example.com"
            - name: DJANGO_PERFORM_MIGRATIONS
              value: "True"
            - name: DJANGO_DB_ENGINE
              value: "django.db.backends.postgresql"
            - name: DJANGO_CACHE_BACKEND
              value: "django_redis.cache.RedisCache"
            - name: DJANGO_CACHE_LOCATION
              value: "redis://golden-redis:6379/1"
            - name: DJANGO_CACHE_CLIENT_CLASS
              value: "django_redis.client.DefaultClient"
            - name: DJANGO_CACHE_TIMEOUT
              value: "1296000"
            - name: EXERCISE_CACHE_TTL
              value: "2419200"
            - name: SITE_URL
              value: "https://golden.example.com"
            - name: CSRF_TRUSTED_ORIGINS
              value: "http://golden.example.com,https://golden.example.com,http://127.0.0.1,https://127.0.0.1,http://localhost,https://localhost"
            - name: DJANGO_DEBUG
              value: "False"
            - name: DJANGO_MEDIA_ROOT
              value: "/home/wger/media"
            - name: NUMBER_OF_PROXIES
              value: "1"
            - name: AXES_ENABLED
              value: "True"
            - name: AXES_LOCKOUT_PARAMETERS
              value: "ip_address"
            - name: AXES_FAILURE_LIMIT
              value: "10"
            - name: AXES_COOLOFF_TIME
              value: "30"
            - name: AXES_IPWARE_PROXY_COUNT
              value: "1"
            - name: AXES_IPWARE_META_PRECEDENCE_ORDER
              value: "HTTP_X_FORWARDED_FOR,REMOTE_ADDR"
            - name: AXES_HANDLER
              value: "axes.handlers.cache.AxesCacheHandler"
            - name: ACCESS_TOKEN_LIFETIME
              value: "10"
            - name: REFRESH_TOKEN_LIFETIME
              value: "24"
            - name: WGER_USE_GUNICORN
              value: "True"
            - name: GUNICORN_CMD_ARGS
              value: "--timeout 240 --workers 4 --worker-class gthread --threads 3 --forwarded-allow-ips * --proxy-protocol True --access-logformat='%(h)s %(l)s %({client-ip}i)s %(l)s %({x-real-ip}i)s %(l)s %({x-forwarded-for}i)s %(l)s %(t)s \"%(r)s\" %(s)s %(b)s \"%(f)s\" \"%(a)s\"' --access-logfile - --error-logfile -"
            - name: MIN_ACCOUNT_AGE_TO_TRUST
              value: "21"
            - name: ALLOW_REGISTRATION
              value: "False"
            - name: ALLOW_GUEST_USERS
              value: "False"
            - name: WGER_INSTANCE
              value: "https://wger.de"
            - name: ALLOW_UPLOAD_VIDEOS
              value: "True"
            - name: SYNC_EXERCISES_ON_STARTUP
              value: "False"
            - name: DOWNLOAD_EXERCISE_IMAGES_ON_STARTUP
              value: "False"
            - name: USE_CELERY
              value: "True"
            - name: SYNC_EXERCISES_CELERY
              value: "true"
            - name: SYNC_EXERCISE_IMAGES_CELERY
              value: "true"
            - name: SYNC_EXERCISE_VIDEOS_CELERY
              value: "true"
            - name: DOWNLOAD_INGREDIENTS_FROM
              value: "WGER"
            - name: CELERY_WORKER_CONCURRENCY
              value: "4"
            - name: CACHE_API_EXERCISES_CELERY
              value: "true"
            - name: CACHE_API_EXERCISES_CELERY_FORCE_UPDATE
              value: "true"          
            - name: DJANGO_DB_HOST
              value: "golden-postgres"
            - name: DJANGO_DB_PORT
              value: "5432"
            - name: DJANGO_DB_USER
              valueFrom:
                secretKeyRef:
                  name:  golden-postgres
                  key: "USERDB_USER"
            - name: DJANGO_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "USERDB_PASSWORD"
            - name: DJANGO_DB_DATABASE
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "POSTGRES_DB"          
            - name: JWT_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "private-key"
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "public-key"
            # This is the path (inside the container) to the YAML config file
            # Alternatively the config path can be specified in the command
            # e.g:
            #   command: ['start', '-r', 'unified', '-c', '/config/powersync.yaml']
            #
            # The config file can also be specified in Base 64 encoding
            # e.g.: Via an environment variable
            #   POWERSYNC_CONFIG_B64: [base64 encoded content]
            # or e.g.: Via a command line parameter
            #    command: ['start', '-r', 'unified', '-c64', '[base64 encoded content]']
            - name: POWERSYNC_CONFIG_PATH
              value: "/config/powersync.yaml"
            # Sync rules can be specified as base 64 encoded YAML
            # e.g: Via an environment variable
            # POWERSYNC_SYNC_RULES_B64: "[base64 encoded sync rules]"
            # or e.g.: Via a command line parameter
            #     command: ['start', '-r', 'unified', '-sync64', '[base64 encoded content]']
            - name: PS_JWKS_URL
              value: "http://golden-http:80/api/v2/powersync-keys"
            - name: PS_PORT
              value: "8080"
            - name: POWERSYNC_URL_PATH
              value: "ps"
            # ps database settings
            - name: PS_DB_USER
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "user"
            - name: PS_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "pw"
            - name: PS_STORAGE_PG_URI
              value: "postgres://$(PS_DB_USER):$(PS_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"
            - name: PS_DATABASE_URI
              value: "postgres://$(DJANGO_DB_USER):$(DJANGO_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"          
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-django"
                  key: "secret-key"
            - name: EMAIL_HOST_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-mail"
                  key: "mail-password"
            - name: DJANGO_CACHE_CLIENT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-redis"
                  key: "redis-password"
            - name: CELERY_BROKER
              value: "redis://:$(DJANGO_CACHE_CLIENT_PASSWORD)@golden-redis:6379/2"
            - name: CELERY_BACKEND
              value: "redis://:$(DJANGO_CACHE_CLIENT_PASSWORD)@golden-redis:6379/2"
            - name: CELERY_FLOWER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-flower"
                  key: "password"
          command: ["/start-flower"]
          ports:
            - name: flower
              containerPort: 5555
              protocol: TCP
      initContainers:
        - name: init-container
          image: docker.io/busybox:1.37
          imagePullPolicy: IfNotPresent
          command:          
          - /bin/sh
          - -c
          # sleep 35; wait for terminationGracePeriodSeconds of the wger-app container
          # this prevents using the wger-app container which are in the process of termination
          # @todo find a better solution to prevent starting powersync
          # on upgrades before the new wger-app container is ready
          # -> this may be only relevant when upgrading from a "non" powersync setup
          - sleep 35; until nc -zvw10 "golden-postgres" "5432"; do echo "Waiting for postgres service ("golden-postgres":"5432") "; sleep 2; done &&
            until nc -zvw10 golden-redis 6379; do echo "Waiting for redis service (golden-redis:6379)"; sleep 2; done &&
            until nc -zvw10 golden-app "8000"; do echo "Waiting for wger app service (golden-app:"8000")"; sleep 2; done
      volumes:
        - name: wger-celery-beat
          persistentVolumeClaim:
            claimName: "golden-celery-beat"
---
# Source: wger/templates/deployment-celery.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: golden-celery-worker
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
spec:
  replicas: 1
  strategy:
    # kills all old Pods first
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: golden-celery-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: golden-celery-worker
      annotations:        
        checksum/secrets: d0c5d15aecc7a1765236648dccd0e733aff527b1194051d18479f566e1bf5c7b
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        # celery worker
        - name: celery-worker
          image: "docker.io/wger/server:2.6"
          imagePullPolicy: IfNotPresent
          env:            
            # get default env
            # get list of custom defined env
            # get default env list without custom ones (override)
            # merge default env with values env
            # ouput list of dict
            - name: TZ
              value: "UTC"
            - name: TIME_ZONE
              value: "UTC"
            - name: ENABLE_EMAIL
              value: "True"
            - name: EMAIL_HOST
              value: "smtp.example.com"
            - name: EMAIL_PORT
              value: "587"
            - name: EMAIL_HOST_USER
              value: "wger@example.com"
            - name: FROM_EMAIL
              value: "wger@example.com"
            - name: DJANGO_PERFORM_MIGRATIONS
              value: "True"
            - name: DJANGO_DB_ENGINE
              value: "django.db.backends.postgresql"
            - name: DJANGO_CACHE_BACKEND
              value: "django_redis.cache.RedisCache"
            - name: DJANGO_CACHE_LOCATION
              value: "redis://golden-redis:6379/1"
            - name: DJANGO_CACHE_CLIENT_CLASS
              value: "django_redis.client.DefaultClient"
            - name: DJANGO_CACHE_TIMEOUT
              value: "1296000"
            - name: EXERCISE_CACHE_TTL
              value: "2419200"
            - name: SITE_URL
              value: "https://golden.example.com"
            - name: CSRF_TRUSTED_ORIGINS
              value: "http://golden.example.com,https://golden.example.com,http://127.0.0.1,https://127.0.0.1,http://localhost,https://localhost"
            - name: DJANGO_DEBUG
              value: "False"
            - name: DJANGO_MEDIA_ROOT
              value: "/home/wger/media"
            - name: NUMBER_OF_PROXIES
              value: "1"
            - name: AXES_ENABLED
              value: "True"
            - name: AXES_LOCKOUT_PARAMETERS
              value: "ip_address"
            - name: AXES_FAILURE_LIMIT
              value: "10"
            - name: AXES_COOLOFF_TIME
              value: "30"
            - name: AXES_IPWARE_PROXY_COUNT
              value: "1"
            - name: AXES_IPWARE_META_PRECEDENCE_ORDER
              value: "HTTP_X_FORWARDED_FOR,REMOTE_ADDR"
            - name: AXES_HANDLER
              value: "axes.handlers.cache.AxesCacheHandler"
            - name: ACCESS_TOKEN_LIFETIME
              value: "10"
            - name: REFRESH_TOKEN_LIFETIME
              value: "24"
            - name: WGER_USE_GUNICORN
              value: "True"
            - name: GUNICORN_CMD_ARGS
              value: "--timeout 240 --workers 4 --worker-class gthread --threads 3 --forwarded-allow-ips * --proxy-protocol True --access-logformat='%(h)s %(l)s %({client-ip}i)s %(l)s %({x-real-ip}i)s %(l)s %({x-forwarded-for}i)s %(l)s %(t)s \"%(r)s\" %(s)s %(b)s \"%(f)s\" \"%(a)s\"' --access-logfile - --error-logfile -"
            - name: MIN_ACCOUNT_AGE_TO_TRUST
              value: "21"
            - name: ALLOW_REGISTRATION
              value: "False"
            - name: ALLOW_GUEST_USERS
              value: "False"
            - name: WGER_INSTANCE
              value: "https://wger.de"
            - name: ALLOW_UPLOAD_VIDEOS
              value: "True"
            - name: SYNC_EXERCISES_ON_STARTUP
              value: "False"
            - name: DOWNLOAD_EXERCISE_IMAGES_ON_STARTUP
              value: "False"
            - name: USE_CELERY
              value: "True"
            - name: SYNC_EXERCISES_CELERY
              value: "true"
            - name: SYNC_EXERCISE_IMAGES_CELERY
              value: "true"
            - name: SYNC_EXERCISE_VIDEOS_CELERY
              value: "true"
            - name: DOWNLOAD_INGREDIENTS_FROM
              value: "WGER"
            - name: CELERY_WORKER_CONCURRENCY
              value: "4"
            - name: CACHE_API_EXERCISES_CELERY
              value: "true"
            - name: CACHE_API_EXERCISES_CELERY_FORCE_UPDATE
              value: "true"          
            - name: DJANGO_DB_HOST
              value: "golden-postgres"
            - name: DJANGO_DB_PORT
              value: "5432"
            - name: DJANGO_DB_USER
              valueFrom:
                secretKeyRef:
                  name:  golden-postgres
                  key: "USERDB_USER"
            - name: DJANGO_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "USERDB_PASSWORD"
            - name: DJANGO_DB_DATABASE
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "POSTGRES_DB"          
            - name: JWT_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "private-key"
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "public-key"
            # This is the path (inside the container) to the YAML config file
            # Alternatively the config path can be specified in the command
            # e.g:
            #   command: ['start', '-r', 'unified', '-c', '/config/powersync.yaml']
            #
            # The config file can also be specified in Base 64 encoding
            # e.g.: Via an environment variable
            #   POWERSYNC_CONFIG_B64: [base64 encoded content]
            # or e.g.: Via a command line parameter
            #    command: ['start', '-r', 'unified', '-c64', '[base64 encoded content]']
            - name: POWERSYNC_CONFIG_PATH
              value: "/config/powersync.yaml"
            # Sync rules can be specified as base 64 encoded YAML
            # e.g: Via an environment variable
            # POWERSYNC_SYNC_RULES_B64: "[base64 encoded sync rules]"
            # or e.g.: Via a command line parameter
            #     command: ['start', '-r', 'unified', '-sync64', '[base64 encoded content]']
            - name: PS_JWKS_URL
              value: "http://golden-http:80/api/v2/powersync-keys"
            - name: PS_PORT
              value: "8080"
            - name: POWERSYNC_URL_PATH
              value: "ps"
            # ps database settings
            - name: PS_DB_USER
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "user"
            - name: PS_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "pw"
            - name: PS_STORAGE_PG_URI
              value: "postgres://$(PS_DB_USER):$(PS_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"
            - name: PS_DATABASE_URI
              value: "postgres://$(DJANGO_DB_USER):$(DJANGO_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"          
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-django"
                  key: "secret-key"
            - name: EMAIL_HOST_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-mail"
                  key: "mail-password"
            - name: DJANGO_CACHE_CLIENT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-redis"
                  key: "redis-password"
            - name: CELERY_BROKER
              value: "redis://:$(DJANGO_CACHE_CLIENT_PASSWORD)@golden-redis:6379/2"
            - name: CELERY_BACKEND
              value: "redis://:$(DJANGO_CACHE_CLIENT_PASSWORD)@golden-redis:6379/2"
            - name: CELERY_FLOWER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-flower"
                  key: "password"
          command: ["/start-worker"]
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - celery -A wger inspect ping
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          volumeMounts:
            - name: wger-media
              mountPath: /home/wger/media
              readOnly: false
      initContainers:
      - name: init-container
        image: docker.io/busybox:1.37
        imagePullPolicy: IfNotPresent
        command:          
          - /bin/sh
          - -c
          # sleep 35; wait for terminationGracePeriodSeconds of the wger-app container
          # this prevents using the wger-app container which are in the process of termination
          # @todo find a better solution to prevent starting powersync
          # on upgrades before the new wger-app container is ready
          # -> this may be only relevant when upgrading from a "non" powersync setup
          - sleep 35; until nc -zvw10 "golden-postgres" "5432"; do echo "Waiting for postgres service ("golden-postgres":"5432") "; sleep 2; done &&
            until nc -zvw10 golden-redis 6379; do echo "Waiting for redis service (golden-redis:6379)"; sleep 2; done &&
            until nc -zvw10 golden-app "8000"; do echo "Waiting for wger app service (golden-app:"8000")"; sleep 2; done
      volumes:
        - name: wger-media
          persistentVolumeClaim:
            claimName: "golden-media"
        - name: wger-celery-beat
          persistentVolumeClaim:
            claimName: "golden-celery-beat"
---
# Source: wger/templates/deployment-nginx.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: golden-nginx
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: golden-nginx
  template:
    metadata:
      labels:
        app.kubernetes.io/name: golden-nginx
      annotations:
        # restart when the mounted nginx config changes
        checksum/config: b6790dc02e101efefdca319984ca01d824c84801eb6be86b68c9b7e2f55ea0c0
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        # nginx container
        - name: nginx
          image: nginx:stable
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: "UTC"
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          volumeMounts:
            - name: nginx-conf
              mountPath: /etc/nginx/conf.d/
            - name: wger-media
              mountPath: /wger/media
              readOnly: true
            - name: wger-static
              mountPath: /wger/static
              readOnly: true
      volumes:
        - name: wger-media
          persistentVolumeClaim:
            claimName: "golden-media"
        - name: wger-static
          persistentVolumeClaim:
            claimName: "golden-static"
        - name: nginx-conf
          configMap:
            name: golden-nginx-configmap
---
# Source: wger/templates/deployment-powersync.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: golden-powersync
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
spec:
  replicas: 1
  strategy:
    # kills all old Pods first
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: golden-powersync
  template:
    metadata:
      labels:
        app.kubernetes.io/name: golden-powersync
      annotations:        
        checksum/secrets: d0c5d15aecc7a1765236648dccd0e733aff527b1194051d18479f566e1bf5c7b
        # restart when the mounted powersync config changes
        checksum/config: b221a56fcd54c90cb20c95572d055a759f0535f9ca43f1984aeabd76a57f4b05
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: powersync
          image: "docker.io/journeyapps/powersync-service:latest"
          imagePullPolicy: IfNotPresent
          env:          
            - name: DJANGO_DB_HOST
              value: "golden-postgres"
            - name: DJANGO_DB_PORT
              value: "5432"
            - name: DJANGO_DB_USER
              valueFrom:
                secretKeyRef:
                  name:  golden-postgres
                  key: "USERDB_USER"
            - name: DJANGO_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "USERDB_PASSWORD"
            - name: DJANGO_DB_DATABASE
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "POSTGRES_DB"          
            - name: JWT_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "private-key"
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "public-key"
            # This is the path (inside the container) to the YAML config file
            # Alternatively the config path can be specified in the command
            # e.g:
            #   command: ['start', '-r', 'unified', '-c', '/config/powersync.yaml']
            #
            # The config file can also be specified in Base 64 encoding
            # e.g.: Via an environment variable
            #   POWERSYNC_CONFIG_B64: [base64 encoded content]
            # or e.g.: Via a command line parameter
            #    command: ['start', '-r', 'unified', '-c64', '[base64 encoded content]']
            - name: POWERSYNC_CONFIG_PATH
              value: "/config/powersync.yaml"
            # Sync rules can be specified as base 64 encoded YAML
            # e.g: Via an environment variable
            # POWERSYNC_SYNC_RULES_B64: "[base64 encoded sync rules]"
            # or e.g.: Via a command line parameter
            #     command: ['start', '-r', 'unified', '-sync64', '[base64 encoded content]']
            - name: PS_JWKS_URL
              value: "http://golden-http:80/api/v2/powersync-keys"
            - name: PS_PORT
              value: "8080"
            - name: POWERSYNC_URL_PATH
              value: "ps"
            # ps database settings
            - name: PS_DB_USER
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "user"
            - name: PS_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "pw"
            - name: PS_STORAGE_PG_URI
              value: "postgres://$(PS_DB_USER):$(PS_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"
            - name: PS_DATABASE_URI
              value: "postgres://$(DJANGO_DB_USER):$(DJANGO_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"
          #
          # In Kubernetes commands overrides the entrypoint and args adds additional arguments to the entrypoint (command)
          # so in this comment "command" is ment do be "args"
          #
          # The unified service runs an API server and replication worker in the same container.
          # These services can be executed in different containers by using individual entry commands e.g.
          # Start only the API server with
          #   command: ['start', '-r', 'api']
          # Start only the replication worker with
          #   command: ['start', '-r', 'sync']

          # Migrations occur automatically by default. Default migrations can be disabled in `powersync.yaml`:
          #  migrations:
          #   disable_auto_migration: true
          #
          # Service migrations can be manually triggered by starting a container with the
          # following command:
          #     command: ['migrate', 'up']
          # Note that this container must finish executing before starting the sync or unified container.
          args: ["start", "-r", "unified"]
          ports:
            - name: api
              containerPort: 8080
              protocol: TCP
            - name: prometheus
              containerPort: 9090
              protocol: TCP
          volumeMounts:
            - name: powersync-conf
              mountPath: /config
      initContainers:
        - name: init-container
          image: docker.io/busybox:1.37
          imagePullPolicy: IfNotPresent
          command:          
          - /bin/sh
          - -c
          # sleep 35; wait for terminationGracePeriodSeconds of the wger-app container
          # this prevents using the wger-app container which are in the process of termination
          # @todo find a better solution to prevent starting powersync
          # on upgrades before the new wger-app container is ready
          # -> this may be only relevant when upgrading from a "non" powersync setup
          - sleep 35; until nc -zvw10 "golden-postgres" "5432"; do echo "Waiting for postgres service ("golden-postgres":"5432") "; sleep 2; done &&
            until nc -zvw10 golden-redis 6379; do echo "Waiting for redis service (golden-redis:6379)"; sleep 2; done &&
            until nc -zvw10 golden-app "8000"; do echo "Waiting for wger app service (golden-app:"8000")"; sleep 2; done
      volumes:
        - name: powersync-conf
          configMap:
            name: golden-powersync-configmap
---
# Source: wger/templates/deployment-wger.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: golden-app
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
spec:
  replicas: 1
  strategy:
    # kills all old Pods first
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: golden-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: golden-app
      annotations:        
        checksum/secrets: d0c5d15aecc7a1765236648dccd0e733aff527b1194051d18479f566e1bf5c7b
    spec:
      # explicit set the default of 30s grace period
      # for the container to be SIGKILL'ed, pevents
      # powersync to start before old containers
      # are removed 
      terminationGracePeriodSeconds: 30
      securityContext:
        fsGroup: 1000
      containers:
        # wger app container
        - name: wger
          image: "docker.io/wger/server:2.6"
          imagePullPolicy: IfNotPresent
          env:            
            # get default env
            # get list of custom defined env
            # get default env list without custom ones (override)
            # merge default env with values env
            # ouput list of dict
            - name: TZ
              value: "UTC"
            - name: TIME_ZONE
              value: "UTC"
            - name: ENABLE_EMAIL
              value: "True"
            - name: EMAIL_HOST
              value: "smtp.example.com"
            - name: EMAIL_PORT
              value: "587"
            - name: EMAIL_HOST_USER
              value: "wger@example.com"
            - name: FROM_EMAIL
              value: "wger@example.com"
            - name: DJANGO_PERFORM_MIGRATIONS
              value: "True"
            - name: DJANGO_DB_ENGINE
              value: "django.db.backends.postgresql"
            - name: DJANGO_CACHE_BACKEND
              value: "django_redis.cache.RedisCache"
            - name: DJANGO_CACHE_LOCATION
              value: "redis://golden-redis:6379/1"
            - name: DJANGO_CACHE_CLIENT_CLASS
              value: "django_redis.client.DefaultClient"
            - name: DJANGO_CACHE_TIMEOUT
              value: "1296000"
            - name: EXERCISE_CACHE_TTL
              value: "2419200"
            - name: SITE_URL
              value: "https://golden.example.com"
            - name: CSRF_TRUSTED_ORIGINS
              value: "http://golden.example.com,https://golden.example.com,http://127.0.0.1,https://127.0.0.1,http://localhost,https://localhost"
            - name: DJANGO_DEBUG
              value: "False"
            - name: DJANGO_MEDIA_ROOT
              value: "/home/wger/media"
            - name: NUMBER_OF_PROXIES
              value: "1"
            - name: AXES_ENABLED
              value: "True"
            - name: AXES_LOCKOUT_PARAMETERS
              value: "ip_address"
            - name: AXES_FAILURE_LIMIT
              value: "10"
            - name: AXES_COOLOFF_TIME
              value: "30"
            - name: AXES_IPWARE_PROXY_COUNT
              value: "1"
            - name: AXES_IPWARE_META_PRECEDENCE_ORDER
              value: "HTTP_X_FORWARDED_FOR,REMOTE_ADDR"
            - name: AXES_HANDLER
              value: "axes.handlers.cache.AxesCacheHandler"
            - name: ACCESS_TOKEN_LIFETIME
              value: "10"
            - name: REFRESH_TOKEN_LIFETIME
              value: "24"
            - name: WGER_USE_GUNICORN
              value: "True"
            - name: GUNICORN_CMD_ARGS
              value: "--timeout 240 --workers 4 --worker-class gthread --threads 3 --forwarded-allow-ips * --proxy-protocol True --access-logformat='%(h)s %(l)s %({client-ip}i)s %(l)s %({x-real-ip}i)s %(l)s %({x-forwarded-for}i)s %(l)s %(t)s \"%(r)s\" %(s)s %(b)s \"%(f)s\" \"%(a)s\"' --access-logfile - --error-logfile -"
            - name: MIN_ACCOUNT_AGE_TO_TRUST
              value: "21"
            - name: ALLOW_REGISTRATION
              value: "False"
            - name: ALLOW_GUEST_USERS
              value: "False"
            - name: WGER_INSTANCE
              value: "https://wger.de"
            - name: ALLOW_UPLOAD_VIDEOS
              value: "True"
            - name: SYNC_EXERCISES_ON_STARTUP
              value: "False"
            - name: DOWNLOAD_EXERCISE_IMAGES_ON_STARTUP
              value: "False"
            - name: USE_CELERY
              value: "True"
            - name: SYNC_EXERCISES_CELERY
              value: "true"
            - name: SYNC_EXERCISE_IMAGES_CELERY
              value: "true"
            - name: SYNC_EXERCISE_VIDEOS_CELERY
              value: "true"
            - name: DOWNLOAD_INGREDIENTS_FROM
              value: "WGER"
            - name: CELERY_WORKER_CONCURRENCY
              value: "4"
            - name: CACHE_API_EXERCISES_CELERY
              value: "true"
            - name: CACHE_API_EXERCISES_CELERY_FORCE_UPDATE
              value: "true"          
            - name: DJANGO_DB_HOST
              value: "golden-postgres"
            - name: DJANGO_DB_PORT
              value: "5432"
            - name: DJANGO_DB_USER
              valueFrom:
                secretKeyRef:
                  name:  golden-postgres
                  key: "USERDB_USER"
            - name: DJANGO_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "USERDB_PASSWORD"
            - name: DJANGO_DB_DATABASE
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "POSTGRES_DB"          
            - name: JWT_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "private-key"
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "public-key"
            # This is the path (inside the container) to the YAML config file
            # Alternatively the config path can be specified in the command
            # e.g:
            #   command: ['start', '-r', 'unified', '-c', '/config/powersync.yaml']
            #
            # The config file can also be specified in Base 64 encoding
            # e.g.: Via an environment variable
            #   POWERSYNC_CONFIG_B64: [base64 encoded content]
            # or e.g.: Via a command line parameter
            #    command: ['start', '-r', 'unified', '-c64', '[base64 encoded content]']
            - name: POWERSYNC_CONFIG_PATH
              value: "/config/powersync.yaml"
            # Sync rules can be specified as base 64 encoded YAML
            # e.g: Via an environment variable
            # POWERSYNC_SYNC_RULES_B64: "[base64 encoded sync rules]"
            # or e.g.: Via a command line parameter
            #     command: ['start', '-r', 'unified', '-sync64', '[base64 encoded content]']
            - name: PS_JWKS_URL
              value: "http://golden-http:80/api/v2/powersync-keys"
            - name: PS_PORT
              value: "8080"
            - name: POWERSYNC_URL_PATH
              value: "ps"
            # ps database settings
            - name: PS_DB_USER
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "user"
            - name: PS_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "pw"
            - name: PS_STORAGE_PG_URI
              value: "postgres://$(PS_DB_USER):$(PS_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"
            - name: PS_DATABASE_URI
              value: "postgres://$(DJANGO_DB_USER):$(DJANGO_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"          
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-django"
                  key: "secret-key"
            - name: EMAIL_HOST_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-mail"
                  key: "mail-password"
            - name: DJANGO_CACHE_CLIENT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-redis"
                  key: "redis-password"
            - name: CELERY_BROKER
              value: "redis://:$(DJANGO_CACHE_CLIENT_PASSWORD)@golden-redis:6379/2"
            - name: CELERY_BACKEND
              value: "redis://:$(DJANGO_CACHE_CLIENT_PASSWORD)@golden-redis:6379/2"
            - name: CELERY_FLOWER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-flower"
                  key: "password"
          ports:
            - name: app
              containerPort: 8000
              protocol: TCP
          # only accept network connection when ready
          # eg. when database migrations and collectstatic
          # have finished
          readinessProbe:
            httpGet:
              path: /
              port: app
            periodSeconds: 5
          resources:
            limits:
              cpu: 500m
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 512Mi
          volumeMounts:
            - name: wger-media
              mountPath: /home/wger/media
              readOnly: false
            - name: wger-static
              mountPath: /home/wger/static
              readOnly: false
            - name: wger-celery-beat
              mountPath: /home/wger/beat
              readOnly: false
      initContainers:
        - name: init-container
          image: docker.io/busybox:1.37
          imagePullPolicy: IfNotPresent
          command:          
          - /bin/sh
          - -c
          - until nc -zvw10 "golden-postgres" "5432"; do echo "Waiting for postgres service ("golden-postgres":"5432") "; sleep 2; done &&
            until nc -zvw10 golden-redis 6379; do echo "Waiting for redis service (golden-redis:6379)"; sleep 2; done
      volumes:
        - name: wger-media
          persistentVolumeClaim:
            claimName: "golden-media"
        - name: wger-static
          persistentVolumeClaim:
            claimName: "golden-static"
        - name: wger-celery-beat
          persistentVolumeClaim:
            claimName: "golden-celery-beat"
---
# Source: wger/charts/postgres/templates/statefulset.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: golden-postgres
  labels:
    helm.sh/chart: postgres-1.3.11
    app.kubernetes.io/name: postgres
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "15.18"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  serviceName: golden-postgres
  podManagementPolicy: OrderedReady
  updateStrategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres
      app.kubernetes.io/instance: golden
  template:
    metadata:
      annotations:
        checksum/customconfig: 01ba4719c80b6fe911b091a7c05124b64eeece964e09c058ef8f9805daca546b
        checksum/secureconfig: 0aafaa74f55e1fb82576b6def31f330e7b84927e8375115ed44553e72c6ae73a
        checksum/customscripts: 01ba4719c80b6fe911b091a7c05124b64eeece964e09c058ef8f9805daca546b
        checksum/scripts: 621d8ca887516cdae2e7e63354f6c479f93d8d1605b7f23201636feaec345699
      labels:
        app.kubernetes.io/name: postgres
        app.kubernetes.io/instance: golden
    spec:
      serviceAccountName: default
      securityContext:
        fsGroup: 999
        supplementalGroups:
        - 999
      initContainers:
        - name: postgres-init
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 999
            runAsNonRoot: true
            runAsUser: 999
          image: "docker.io/postgres:15.18"
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - mountPath: /extrascripts
              name: extrascripts-volume
            - mountPath: /initscripts
              name: initscripts
            - mountPath: /scripts
              name: scripts
            - mountPath: /configs
              name: configs
          command: [ "/initscripts/init.sh" ]
      containers:
        - name: postgres
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 999
            runAsNonRoot: true
            runAsUser: 999
          image: "docker.io/postgres:15.18"
          imagePullPolicy: IfNotPresent
          ports:
            - name: postgres
              containerPort: 5432
              protocol: TCP
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: POSTGRES_DB
            - name: USERDB_USER
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: USERDB_USER
            - name: USERDB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: USERDB_PASSWORD
            - name: PGDATA
              value: "/var/lib/postgresql/data/pg"
          startupProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -h localhost
            initialDelaySeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
            successThreshold: 1
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -h localhost
            initialDelaySeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -h localhost
            initialDelaySeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
            periodSeconds: 10
          resources:
            limits:
              cpu: 500m
              memory: 1280Mi
            requests:
              cpu: 100m
              memory: 1032Mi
          args:
            - -c
            - wal_level=logical
            - -c
            - shared_buffers=256MB
            - -c
            - effective_cache_size=768MB
            - -c
            - work_mem=8MB
            - -c
            - random_page_cost=1.1
            - -c
            - max_connections=30
          volumeMounts:
            - mountPath: /var/run
              name: run
            - mountPath: /tmp
              name: tmp
            - mountPath: /var/lib/postgresql/data
              name: postgres-data
            - mountPath: /docker-entrypoint-initdb.d
              name: scripts
            - mountPath: /etc/postgresql
              name: configs
      volumes:
        - name: run
          emptyDir: {}
        - name: tmp
          emptyDir: {}
        - name: scripts
          emptyDir: {}
        - name: configs
          emptyDir: {}
        - name: initscripts
          configMap:
            name: golden-postgres-scripts
            defaultMode: 0555
        - name: extrascripts-volume
          configMap:
            name: wger-pg-init
            defaultMode: 0555
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 8Gi
---
# Source: wger/templates/cronjob-powersync-compact.yaml
# daily compaction of the powersync bucket storage. Self-hosted instances are
# not compacted automatically; the `compact` command of the service image
# processes all buckets using the same config and storage as the service.
# (`powersync compact` of the powersync-cli only supports PowerSync Cloud)
# See https://docs.powersync.com/maintenance-ops/compacting-buckets
apiVersion: batch/v1
kind: CronJob
metadata:
  name: golden-powersync-compact
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
spec:
  schedule: "0 3 * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        metadata:
          labels:
            app.kubernetes.io/name: golden-powersync-compact
        spec:
          restartPolicy: Never
          securityContext:
            fsGroup: 1000
          containers:
            - name: powersync-compact
              image: "docker.io/journeyapps/powersync-service:latest"
              imagePullPolicy: IfNotPresent
              env:              
                - name: DJANGO_DB_HOST
                  value: "golden-postgres"
                - name: DJANGO_DB_PORT
                  value: "5432"
                - name: DJANGO_DB_USER
                  valueFrom:
                    secretKeyRef:
                      name:  golden-postgres
                      key: "USERDB_USER"
                - name: DJANGO_DB_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: golden-postgres
                      key: "USERDB_PASSWORD"
                - name: DJANGO_DB_DATABASE
                  valueFrom:
                    secretKeyRef:
                      name: golden-postgres
                      key: "POSTGRES_DB"              
                - name: JWT_PRIVATE_KEY
                  valueFrom:
                    secretKeyRef:
                      name: "golden-jwt"
                      key: "private-key"
                - name: JWT_PUBLIC_KEY
                  valueFrom:
                    secretKeyRef:
                      name: "golden-jwt"
                      key: "public-key"
                # This is the path (inside the container) to the YAML config file
                # Alternatively the config path can be specified in the command
                # e.g:
                #   command: ['start', '-r', 'unified', '-c', '/config/powersync.yaml']
                #
                # The config file can also be specified in Base 64 encoding
                # e.g.: Via an environment variable
                #   POWERSYNC_CONFIG_B64: [base64 encoded content]
                # or e.g.: Via a command line parameter
                #    command: ['start', '-r', 'unified', '-c64', '[base64 encoded content]']
                - name: POWERSYNC_CONFIG_PATH
                  value: "/config/powersync.yaml"
                # Sync rules can be specified as base 64 encoded YAML
                # e.g: Via an environment variable
                # POWERSYNC_SYNC_RULES_B64: "[base64 encoded sync rules]"
                # or e.g.: Via a command line parameter
                #     command: ['start', '-r', 'unified', '-sync64', '[base64 encoded content]']
                - name: PS_JWKS_URL
                  value: "http://golden-http:80/api/v2/powersync-keys"
                - name: PS_PORT
                  value: "8080"
                - name: POWERSYNC_URL_PATH
                  value: "ps"
                # ps database settings
                - name: PS_DB_USER
                  valueFrom:
                    secretKeyRef:
                      name: "golden-powersync"
                      key: "user"
                - name: PS_DB_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: "golden-powersync"
                      key: "pw"
                - name: PS_STORAGE_PG_URI
                  value: "postgres://$(PS_DB_USER):$(PS_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"
                - name: PS_DATABASE_URI
                  value: "postgres://$(DJANGO_DB_USER):$(DJANGO_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"
              args: ["compact"]
              volumeMounts:
                - name: powersync-conf
                  mountPath: /config
          volumes:
            - name: powersync-conf
              configMap:
                name: golden-powersync-configmap
---
# Source: wger/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: golden-ingress
  namespace: golden-ns
spec:
  tls:
    - hosts:
        - golden.example.com
      secretName: golden-tls
  rules:
    - host: golden.example.com
      http:
        paths:
        - pathType: Prefix
          path: /
          backend:
            service:
              name: golden-http
              port:
                number: 80
---
# Source: wger/templates/serviceaccount.yaml
# for keygen, currently only used for jwt key generation
apiVersion: v1
kind: ServiceAccount
metadata:
  name: golden-keygen
  namespace: golden-ns
  annotations:
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-6"
---
# Source: wger/templates/secret-django.yaml
apiVersion: v1
kind: Secret
metadata:
  name: golden-django
  namespace: golden-ns
  annotations:
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-5"
stringData:
  secret-key: "golden-django-secret-key"
---
# Source: wger/templates/secret-flower.yaml
apiVersion: v1
kind: Secret
metadata:
  name: golden-flower
  namespace: golden-ns
  annotations:
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-5"
stringData:
  password: "golden-flower-password"
---
# Source: wger/templates/secret-mail.yaml
# updates or creates the secret
# manipulatemail defined in _helpers.tpl
apiVersion: v1
kind: Secret
metadata:
  name: golden-mail
  namespace: golden-ns
  annotations:
    helm.sh/resource-policy: keep
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-5"
data:
  mail-password: "Z29sZGVuLW1haWwtcGFzc3dvcmQ="
---
# Source: wger/templates/secret-powersync.yaml
apiVersion: v1
kind: Secret
metadata:
  name: golden-powersync
  namespace: golden-ns
  annotations:
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-5"
stringData:
  user: "powersync_storage"
  pw: "golden-powersync-pw"
---
# Source: wger/templates/secret-redis.yaml
apiVersion: v1
kind: Secret
metadata:
  name: golden-redis
  namespace: golden-ns
  annotations:
    helm.sh/resource-policy: keep
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-5"
stringData:
  redis-password: "golden-redis-password"
---
# Source: wger/templates/role.yaml
# manage secret permission - for keygen, currently used for jwt key generation
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: golden-secret-role
  namespace: golden-ns
  annotations:
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-6"
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["create", "patch", "update", "get"]
---
# Source: wger/templates/rolebinding.yaml
# for keygen, currently only used for jwt key generation
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: golden-keygen
  namespace: golden-ns
  annotations:
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-6"
subjects:
  - kind: ServiceAccount
    name: golden-keygen
roleRef:
  kind: Role
  name: golden-secret-role
  apiGroup: rbac.authorization.k8s.io
---
# Source: wger/templates/hooks/jwt-keygen.yaml
# updates or creates the jwt secret
# manipulatejwt defined in _helpers.tpl
apiVersion: batch/v1
kind: Job
metadata:
  name: golden-keygen
  labels:
    app.kubernetes.io/name: golden
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-5"
    helm.sh/hook-delete-policy: hook-succeeded
spec:
  template:
    spec:
      restartPolicy: Never
      serviceAccountName: golden-keygen
      containers:
        - name: keygen
          image: docker.io/alpine:3.22
          command:
            - sh
            - -c
            - |
              # Add required packages
              apk add jose python3 kubectl
              
              # Create jwk keys
              # https://github.com/wger-project/wger/blob/master/wger/core/management/commands/generate-jwt-keys.py
              # https://datatracker.ietf.org/doc/html/rfc7519
              jose jwk gen -i '{"alg":"RS256", "kid":"wger"}' -o /tmp/jwt.key
              jose jwk pub -i /tmp/jwt.key > /tmp/jwt.pub
              
              # python for json dump string conversion and urlsafe base64 conversion (padding)
              PRIVATEKEY=$(python -c "import json; from base64 import urlsafe_b64encode; print(urlsafe_b64encode(json.dumps($(cat /tmp/jwt.key)).encode()).decode())")
              PUBLICKEY=$(python -c "import json; from base64 import urlsafe_b64encode; print(urlsafe_b64encode(json.dumps($(cat /tmp/jwt.pub)).encode()).decode())")

              # Create temp yaml
              yaml_tmp='
              apiVersion: v1
              kind: Secret
              metadata:
                name: golden-jwt
                namespace: golden-ns
              stringData:
                private-key: "NEW_PRIVATEKEY"
                public-key: "NEW_PUBLICKEY"
              '

              # Add keys to the yaml
              yaml=$(echo "$yaml_tmp" | sed 's/"NEW_PRIVATEKEY"/'$PRIVATEKEY'/g' | sed 's/"NEW_PUBLICKEY"/'$PUBLICKEY'/g')
              # Update or create the secret, this requires a serviceaccount with permissions
              echo "$yaml" | kubectl apply -f -
---
# Source: wger/templates/hooks/setup-powersync-storage.yaml
# post-install hook: initialize the powersync storage schema/user in the DB
# by running `./manage.py setup-powersync-storage` in its own pod, using the
# same image and environment as the wger app container. Only needs database
# access — no ServiceAccount/RBAC.
apiVersion: batch/v1
kind: Job
metadata:
  name: golden-setup-powersync-storage
  namespace: golden-ns
  labels:
    app.kubernetes.io/name: golden
    app.kubernetes.io/instance: golden
    app.kubernetes.io/version: "2.6"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: wger-1.0.0
  annotations:
    helm.sh/hook: post-install
    helm.sh/hook-weight: "0"
    helm.sh/hook-delete-policy: hook-succeeded
spec:
  backoffLimit: 5
  template:
    metadata:
      labels:
        app.kubernetes.io/name: golden-setup-powersync-storage
    spec:
      restartPolicy: Never
      securityContext:
        fsGroup: 1000
      containers:
        - name: setup-powersync-storage
          image: "docker.io/wger/server:2.6"
          imagePullPolicy: IfNotPresent
          command: ["./manage.py", "setup-powersync-storage"]
          env:            
            # get default env
            # get list of custom defined env
            # get default env list without custom ones (override)
            # merge default env with values env
            # ouput list of dict
            - name: TZ
              value: "UTC"
            - name: TIME_ZONE
              value: "UTC"
            - name: ENABLE_EMAIL
              value: "True"
            - name: EMAIL_HOST
              value: "smtp.example.com"
            - name: EMAIL_PORT
              value: "587"
            - name: EMAIL_HOST_USER
              value: "wger@example.com"
            - name: FROM_EMAIL
              value: "wger@example.com"
            - name: DJANGO_PERFORM_MIGRATIONS
              value: "True"
            - name: DJANGO_DB_ENGINE
              value: "django.db.backends.postgresql"
            - name: DJANGO_CACHE_BACKEND
              value: "django_redis.cache.RedisCache"
            - name: DJANGO_CACHE_LOCATION
              value: "redis://golden-redis:6379/1"
            - name: DJANGO_CACHE_CLIENT_CLASS
              value: "django_redis.client.DefaultClient"
            - name: DJANGO_CACHE_TIMEOUT
              value: "1296000"
            - name: EXERCISE_CACHE_TTL
              value: "2419200"
            - name: SITE_URL
              value: "https://golden.example.com"
            - name: CSRF_TRUSTED_ORIGINS
              value: "http://golden.example.com,https://golden.example.com,http://127.0.0.1,https://127.0.0.1,http://localhost,https://localhost"
            - name: DJANGO_DEBUG
              value: "False"
            - name: DJANGO_MEDIA_ROOT
              value: "/home/wger/media"
            - name: NUMBER_OF_PROXIES
              value: "1"
            - name: AXES_ENABLED
              value: "True"
            - name: AXES_LOCKOUT_PARAMETERS
              value: "ip_address"
            - name: AXES_FAILURE_LIMIT
              value: "10"
            - name: AXES_COOLOFF_TIME
              value: "30"
            - name: AXES_IPWARE_PROXY_COUNT
              value: "1"
            - name: AXES_IPWARE_META_PRECEDENCE_ORDER
              value: "HTTP_X_FORWARDED_FOR,REMOTE_ADDR"
            - name: AXES_HANDLER
              value: "axes.handlers.cache.AxesCacheHandler"
            - name: ACCESS_TOKEN_LIFETIME
              value: "10"
            - name: REFRESH_TOKEN_LIFETIME
              value: "24"
            - name: WGER_USE_GUNICORN
              value: "True"
            - name: GUNICORN_CMD_ARGS
              value: "--timeout 240 --workers 4 --worker-class gthread --threads 3 --forwarded-allow-ips * --proxy-protocol True --access-logformat='%(h)s %(l)s %({client-ip}i)s %(l)s %({x-real-ip}i)s %(l)s %({x-forwarded-for}i)s %(l)s %(t)s \"%(r)s\" %(s)s %(b)s \"%(f)s\" \"%(a)s\"' --access-logfile - --error-logfile -"
            - name: MIN_ACCOUNT_AGE_TO_TRUST
              value: "21"
            - name: ALLOW_REGISTRATION
              value: "False"
            - name: ALLOW_GUEST_USERS
              value: "False"
            - name: WGER_INSTANCE
              value: "https://wger.de"
            - name: ALLOW_UPLOAD_VIDEOS
              value: "True"
            - name: SYNC_EXERCISES_ON_STARTUP
              value: "False"
            - name: DOWNLOAD_EXERCISE_IMAGES_ON_STARTUP
              value: "False"
            - name: USE_CELERY
              value: "True"
            - name: SYNC_EXERCISES_CELERY
              value: "true"
            - name: SYNC_EXERCISE_IMAGES_CELERY
              value: "true"
            - name: SYNC_EXERCISE_VIDEOS_CELERY
              value: "true"
            - name: DOWNLOAD_INGREDIENTS_FROM
              value: "WGER"
            - name: CELERY_WORKER_CONCURRENCY
              value: "4"
            - name: CACHE_API_EXERCISES_CELERY
              value: "true"
            - name: CACHE_API_EXERCISES_CELERY_FORCE_UPDATE
              value: "true"          
            - name: DJANGO_DB_HOST
              value: "golden-postgres"
            - name: DJANGO_DB_PORT
              value: "5432"
            - name: DJANGO_DB_USER
              valueFrom:
                secretKeyRef:
                  name:  golden-postgres
                  key: "USERDB_USER"
            - name: DJANGO_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "USERDB_PASSWORD"
            - name: DJANGO_DB_DATABASE
              valueFrom:
                secretKeyRef:
                  name: golden-postgres
                  key: "POSTGRES_DB"          
            - name: JWT_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "private-key"
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-jwt"
                  key: "public-key"
            # This is the path (inside the container) to the YAML config file
            # Alternatively the config path can be specified in the command
            # e.g:
            #   command: ['start', '-r', 'unified', '-c', '/config/powersync.yaml']
            #
            # The config file can also be specified in Base 64 encoding
            # e.g.: Via an environment variable
            #   POWERSYNC_CONFIG_B64: [base64 encoded content]
            # or e.g.: Via a command line parameter
            #    command: ['start', '-r', 'unified', '-c64', '[base64 encoded content]']
            - name: POWERSYNC_CONFIG_PATH
              value: "/config/powersync.yaml"
            # Sync rules can be specified as base 64 encoded YAML
            # e.g: Via an environment variable
            # POWERSYNC_SYNC_RULES_B64: "[base64 encoded sync rules]"
            # or e.g.: Via a command line parameter
            #     command: ['start', '-r', 'unified', '-sync64', '[base64 encoded content]']
            - name: PS_JWKS_URL
              value: "http://golden-http:80/api/v2/powersync-keys"
            - name: PS_PORT
              value: "8080"
            - name: POWERSYNC_URL_PATH
              value: "ps"
            # ps database settings
            - name: PS_DB_USER
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "user"
            - name: PS_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-powersync"
                  key: "pw"
            - name: PS_STORAGE_PG_URI
              value: "postgres://$(PS_DB_USER):$(PS_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"
            - name: PS_DATABASE_URI
              value: "postgres://$(DJANGO_DB_USER):$(DJANGO_DB_PASSWORD)@$(DJANGO_DB_HOST):$(DJANGO_DB_PORT)/$(DJANGO_DB_DATABASE)"          
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: "golden-django"
                  key: "secret-key"
            - name: EMAIL_HOST_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-mail"
                  key: "mail-password"
            - name: DJANGO_CACHE_CLIENT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-redis"
                  key: "redis-password"
            - name: CELERY_BROKER
              value: "redis://:$(DJANGO_CACHE_CLIENT_PASSWORD)@golden-redis:6379/2"
            - name: CELERY_BACKEND
              value: "redis://:$(DJANGO_CACHE_CLIENT_PASSWORD)@golden-redis:6379/2"
            - name: CELERY_FLOWER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "golden-flower"
                  key: "password"
      initContainers:
        # wait for postgres, redis and a ready wger app pod — the app service
        # only gets endpoints once the django migrations have run
        - name: init-container
          image: docker.io/busybox:1.37
          imagePullPolicy: IfNotPresent
          command:          
          - /bin/sh
          - -c
          # sleep 35; wait for terminationGracePeriodSeconds of the wger-app container
          # this prevents using the wger-app container which are in the process of termination
          # @todo find a better solution to prevent starting powersync
          # on upgrades before the new wger-app container is ready
          # -> this may be only relevant when upgrading from a "non" powersync setup
          - sleep 35; until nc -zvw10 "golden-postgres" "5432"; do echo "Waiting for postgres service ("golden-postgres":"5432") "; sleep 2; done &&
            until nc -zvw10 golden-redis 6379; do echo "Waiting for redis service (golden-redis:6379)"; sleep 2; done &&
            until nc -zvw10 golden-app "8000"; do echo "Waiting for wger app service (golden-app:"8000")"; sleep 2; done
