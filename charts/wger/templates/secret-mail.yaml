# updates or creates the secret
# manipulatemail defined in _helpers.tpl
{{- if .Values.app.mail.enabled }}
{{- $manipulatemail := (include "manipulatemail" .) }}
{{- if eq $manipulatemail "doit" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "wger.secretName.mail" . }}
  namespace: {{ .Release.Namespace }}
  annotations:
    helm.sh/resource-policy: keep
    helm.sh/hook: pre-install,pre-upgrade,pre-rollback
    helm.sh/hook-weight: "-5"
data:
  {{ .Values.app.mail.secret.key }}: {{ .Values.app.mail.secret.password | b64enc | quote }}
{{- end }}
{{- end }}
